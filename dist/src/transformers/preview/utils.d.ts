import Handoff from '../../index';
/**
 * Get the component template
 * @param component
 * @param parts
 * @returns
 */
export declare const getComponentTemplate: (handoff: Handoff, component: string, parts: string[]) => Promise<string | null>;
